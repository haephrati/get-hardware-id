# get-hardware-id

MFC GetMacAddress dialog that shows a MAC identifier (`IDC_MACID`) plus a hard-drive serial helper.

## Unit tests

`Tests/MacIdTests` checks about/dialog resource IDs and `IDC_MACID`. Build Release|x64.
